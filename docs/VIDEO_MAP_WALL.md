# Video Screen and Map Wall

## Parent Integration

This change intentionally does not edit main.js, pack manifests, shared sounds,
language files, or the item catalog. Parent integration must:

1. Add `import "./wall_displays.js";` once to
   `minecraft/behavior_packs/import_structures/scripts/main.js`.
2. Merge `minecraft/video_screen/sound_definitions.fragment.json` into the
   `sound_definitions` object of
   `minecraft/resource_packs/ichiyon_avatar_rp/sounds/sound_definitions.json`,
   preserving every existing entry. Exact addition:

   ```json
   "ichiyon.video_screen.audio": {
     "category": "record",
     "min_distance": 1,
     "max_distance": 16,
     "sounds": [{"name": "sounds/video_screen/audio", "stream": false, "is3D": true}]
   }
   ```

3. Include the new files in the existing BP/RP pack builder and bump pack
   versions through the existing release process. Existing import_structures
   dependency `@minecraft/server` `2.11.0-beta` is sufficient for BDS 1.26.51.1.
   No new manifest module, dependency, custom item, creative item, or lang entry
   is needed. The one helper entity is not spawn-egg accessible.

No Git staging, commits, remote operations, production changes, or world changes
were performed by this implementation task. The parent owns integration and BDS
smoke testing. Node mocks are not a Bedrock content-log or client-render test.

## Regeneration

Prerequisites: Python, Pillow, ffmpeg, ffprobe, Node (for runtime tests).

```powershell
python scripts/build_minecraft_video.py 'C:/Users/syoub/Desktop/神社/mp4/3851.mp4'
python scripts/check_minecraft_wall_displays.py --source 'C:/Users/syoub/Desktop/神社/mp4/3851.mp4'
node scripts/check_minecraft_wall_displays.mjs
```

The builder rejects a source located in the repository or output tree. It decodes
frames and PCM in an external temporary directory, never copies the MP4 into the
repository, derives frame count from actual decoded output, and writes a source
SHA-256 plus measured dimensions/rate/durations to
`minecraft/video_screen/build_report.json`. OGG output uses FFmpeg bitexact flags.
Pillow/FFmpeg version changes can still change generated bytes; pixel/audio
comparison should precede asset churn.

Measured source: 640x360, 30000/1001 fps, video 104.4043 s, container/audio about
104.420136 s. Output: 20 fps, 2,088 frames, 104.4 s loop, 21 RGB atlases at
660x380, one real mono OGG. Media totals 9,418,693 bytes with this toolchain.
Each 64x36 frame has one-pixel extruded padding. The final audio is trimmed/padded
to exactly the video loop period. A truly silent source produces no audio entry,
not a fake silent replacement track.

## Detection and Controls

All installation coordinates are in `wall_displays_config.js`.

- Video: X132..142 inclusive, candidate Z -87/-86/-85; bottom Y251..260.
  Detect exactly one exposed, solid, same-material 11x4 rectangle, bounded by a
  different material or air on every edge. Its south-facing air cells must be
  unobstructed. Larger uniform walls, mixed-material rectangles, multiple
  candidates, unloaded blocks, or no match fail closed. This deliberately does
  not guess the real installation from coordinates alone. If production uses a
  mixed-material or unbounded screen, inspect its actual blocks before changing
  the detection rule; no remote screen inspection was performed here.
- One entity at the detected lower center renders an 11x4 black surface plus a
  centered 7.111...x4 video plane. The video remains 16:9 with left/right black
  bands. OFF hides the video plane, retaining black. No underlying block changes.
- Video button: `(131, detectedBottom, detectedWallZ + 1)`.
- Map: actual `minecraft:frame` / `minecraft:glow_frame` BLOCKS, not Java-style
  frame entities. Candidate frame planes Z243..245, bottom Y90..98. Facing must
  be north (`facing_direction=2`), map bit true, support block immediately south.
  X15,14,13 run left-to-right; eight positions are required with X13/top absent.
- Map button: `(16, detectedBottom, detectedFrameZ)`. Press immediately re-scans
  and reports the real result. A last-proven button remains usable while a frame
  is missing, so restoring the frame then pressing works immediately.
- Both use `world.afterEvents.buttonPush` and accept every vanilla `*_button`.
  Buttons are neither placed nor modified; their blocks hold no playback state.
  Removing/replacing a button does not toggle playback. Periodic scans retry
  unloaded installations every 100 ticks.

Ready logs include dimension, detected bottom, wall plane, and computed button
coordinates on first detection, coordinate changes, or recovery after detection
was lost. Map logs also distinguish the frame plane from its supporting wall
plane and include the eight-frame count. Unchanged periodic scans are silent.
Coordinates in these logs come from detection, not an assumed installation Y.

## Playback and Audio

Volatile cold state is OFF. Every ON starts frame zero. One server-synchronized
integer entity property selects the frame, atlas, and UV. A real-time media clock
keeps visual position aligned with audio even when server ticks slow down.
Playback wraps to zero at the measured output duration. OFF resets position and
stops each retained sound handle. Missing/unloaded helper entities fail to OFF;
chunk reload recovers to OFF without duplicate helpers. Script startup removes
old display helpers and clears only the video's sound ID on each player.

Audio uses only `Player.playSound`, never world/dimension broadcast or global
music APIs. Each eligible player owns a retained SoundInstance. Joiners start
muted, seek to the current position, and unmute. The front (+Z) hemisphere within
16 blocks of the screen center is eligible; volume decreases with distance and
is updated as players move. Exiting/dimension change/death/logout stops that
player's handle. Loop boundaries recreate handles at the current media position.
No undocumented seek parameter is passed to playSound. Unsupported SoundInstance
methods fail explicitly and stop audio rather than pretending synchronization.

## Version-Matched API Evidence

Verified the Microsoft-published npm package
`@minecraft/server@2.11.0-beta.1.26.51-stable`, not just the moving online docs.
Its `index.d.ts` declares:

- line 18339: `Player.playSound(...): SoundInstance`.
- line 18661: `Player.stopSound(soundId: string): void`.
- lines 22479..22591: SoundInstance with `seekTo(seconds)`, `setVolume(volume)`,
  and `stop()`; retain handles for controllable playback.
- line 4060: `Block.isSolid`; line 6634: ButtonPushAfterEvent with a source;
  BlockEvent supplies the block/dimension.

Local extracted evidence (not committed):
`C:/Users/syoub/AppData/Local/Temp/ichiyon-video-api-12651/package/index.d.ts`.
The local Docker daemon was unavailable, so this is declaration evidence only,
not proof from the installed BDS image. Parent should validate the existing
stopped `mokuro-smoke-20260923` environment when available. In particular check
SoundInstance seek behavior, RP material/UV orientation, and the content log.

Primary references consulted 2026-09-24:

- [Vanilla blocks and frame states](https://learn.microsoft.com/en-us/minecraft/creator/reference/content/vanillalistingsreference/blocks?view=minecraft-bedrock-stable)
- [Intrinsic block states](https://learn.microsoft.com/en-us/minecraft/creator/reference/content/blockreference/examples/intrinsicblockstateslist?view=minecraft-bedrock-stable)
- [Player sound methods](https://learn.microsoft.com/en-us/minecraft/creator/scriptapi/minecraft/server/player?view=minecraft-bedrock-experimental)
- [PlayerSoundOptions](https://learn.microsoft.com/en-us/minecraft/creator/scriptapi/minecraft/server/playersoundoptions?view=minecraft-bedrock-stable)
- [Official UV atlas material pattern](https://learn.microsoft.com/en-us/minecraft/creator/documents/practices/improvingperformanceandresourceusage?view=minecraft-bedrock-stable)
- [Render controller schema](https://learn.microsoft.com/en-us/minecraft/creator/reference/content/visualreference/render_controller.v1.8.0?view=minecraft-bedrock-stable)
- [Mojang sound schema](https://github.com/Mojang/bedrock-schemas/blob/main/schemas/rp/sounds/index.schema.json)

## Map Recalculation Limitation

There is no documented public operation for recalculating the terrain pixels of
an existing filled map in the reviewed 2.11-beta declarations, block/item
component listings, or vanilla command reference. BlockMapColor changes a block's
map color, not a filled map's pixels. ExplorationMapFunction creates loot maps,
not an update of the original map. We therefore implement detection and re-scan
only. No private NBT, invented API, map replacement, frame destruction, or fake
success indicator is used.

- [Public block components](https://learn.microsoft.com/en-us/minecraft/creator/scriptapi/minecraft/server/blockcomponenttypes?view=minecraft-bedrock-experimental)
- [Vanilla command reference](https://learn.microsoft.com/en-us/minecraft/creator/commands/commands?view=minecraft-bedrock-stable)

## Local Verification

The tests cover actual FFmpeg decoding/encoding, source hash, original rebuild,
PNG pixel identity, audio byte identity, non-silent mono PCM, loop duration,
atlas padding, texture references, entity/geometry contracts, and seventeen
dependency-injected runtime scenarios. No live-world success is claimed.

Prepared outside the checkout, not executed:
`C:/Users/syoub/AppData/Local/Temp/ichiyon-wall-smoke-20260924/`.
Its `prepare.py` snapshots a minimal standalone BP/RP and writes
`wall-smoke-prepared.zip`; `README.md` contains coordinated disposable-world
instructions. The pack is inert until the explicit
`scriptevent ichiyon_wall_smoke:run disposable-only` command. It uses the
version-matched top-level GameTest `spawnSimulatedPlayer` API and native button
interaction, entity properties, SoundInstance methods, and synthetic frame
permutations. Do not run while another worker owns the test container. A client
content-log/render check remains necessary for the RP material/controller.
